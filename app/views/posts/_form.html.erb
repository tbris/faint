<%= form_with(model: post) do |form| %>
  <table id="post-input">
    <tbody>
      <tr>
        <td>
          <%= form.text_area :content %>
        </td>
      </tr>
      <tr>
        <td>
          <%= form.submit "Post" %>
        </td>
      </tr>
  </table>
<% end %>
